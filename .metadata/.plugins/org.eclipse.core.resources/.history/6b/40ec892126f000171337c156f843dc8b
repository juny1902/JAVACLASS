
public class MainClass {
	public static void byAbs(Abstraction a) { // 추상 클래스를 매개변수로 받음.
		a.doYourself();
	}
	public static void giveYourself(Introdution a) { // 추상 클래스를 매개변수로 받음.
		a.printIntroduce();
	}

	public static void main(String[] args) {
		Implementation i = new Implementation();
		MySelf m = new MySelf();
	
		byAbs(i);
		byAbs(m);
		
		i.setName("Ryan");
		m.setName("Parker");
		
		giveYourself(i);
		giveYourself(m);
	}
	
	
}
